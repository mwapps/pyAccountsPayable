Delete a company by Id
---
tags:
  - Company

responses:
  200:
    description: When a delete request is successfully
    schema:
      type: "object"
      properties:
        message:
          type: "string"
          example: "Company was deleted"

  404:
    description: Company not found
    schema:
      $ref: "#/definitions/error"

